/* Function Scope */
function greeting() {
    let userName = 'Martin';
    console.log(userName);

    if (userName === 'Martin') {
        console.log(`Hello ${userName}!`)
    }
}

greeting();
console.log(userName);